# vars/tokens.nix.template — API Token 模板（Phase 1 过渡用）
#
# ⛔  DEPRECATED: 请优先使用 agenix（Phase 2）。
#    设计文档：common/secrets/README.md
#
# 此模板仅供过渡期使用。复制为 tokens.nix 并填入真实值：
#   cp vars/tokens.nix.template vars/tokens.nix
#
# ⚠️  tokens.nix 已加入 .gitignore，切勿提交！
{ ... }:
{
  myHome.ai.tokens = {
    anthropic = "";        # sk-ant-api03-...
    deepseek = "";         # sk-...
    openai = "";           # sk-proj-...
  };
}
